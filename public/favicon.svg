<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#18181b" />
  <!-- Baseline sits at half the cap height below centre (Georgia cap height ~0.69em),
       so the S reads centred rather than sitting low. -->
  <text
    x="16"
    y="22.9"
    font-family="Georgia, 'Times New Roman', serif"
    font-size="20"
    fill="#fafafa"
    text-anchor="middle"
  >S</text>
</svg>
